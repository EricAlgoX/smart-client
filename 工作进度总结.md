# SmartClient 性能优化工作进度

## 已完成的工作

### 1. 代码审查和修复（已完成）
- ✅ 修复 `partial[None]` 语法错误 → `partial`
- ✅ 删除 client.py 中的多余逗号（polygon, confidence, timeout_mins）
- ✅ 修复 'socre' 拼写错误 → 'score'
- ✅ 初始化 `self.frame = None` 和 `self.converter = None`
- ✅ 添加 converter 线程管理，避免线程泄漏

### 2. 性能监控系统（已完成）
- ✅ 创建 `utils/profiler.py` 性能分析工具
- ✅ 在关键方法添加 `@profiler.measure()` 装饰器
- ✅ 监控方法：next, display, tabel_list, postprocess, API请求, JPG编码

### 3. UI 功能增强（已完成）
- ✅ 添加"文件夹"按钮到 UI（mainwindow.ui）
- ✅ 绑定按钮事件到 controller

### 4. 初步性能优化（已完成）
- ✅ 字体缓存：Visualizer 中添加 `_font_cache`
- ✅ 降低视频帧率：stream 模式从 33ms 改为 100ms（10fps）
- ✅ 增加队列缓冲：从 maxsize=1 改为 maxsize=3
- ✅ 降低 JPEG 质量：从 80 改为 60
- ✅ 增加 API 超时：从 (1,3) 改为 (2,10) 秒
- ✅ 图像缩放：客户端缩小到 640px 再发送

### 5. 性能瓶颈定位（已完成）
**性能数据：**
- next: 333ms
- postprocess: 279ms ← **主要瓶颈**
- display: 1.2ms
- tabel_list: 0.9ms

**结论：** 问题在 postprocess 中的图像裁剪和颜色转换，不是 QT 界面问题

---

## 正在进行的工作

### 6. 优化 postprocess 性能（进行中）

**问题：** postprocess 中每个检测框都要裁剪和转换图像，导致 279ms

**已完成的修改：**
1. ✅ client.py：移除 `instance['image'] = cv2.cvtColor(...)`
   - 改为只保存 `instance['bbox'] = (xmin, ymin, xmax, ymax)`

2. ✅ converter.py：修改信号传递
   - 从 `pixmap_ready(QImage, details, path)`
   - 改为 `pixmap_ready(QImage, details, path, original_rgb)`
   - 在 emit 时传递原始 RGB 图像

**还需要完成：**
1. ⏳ 修改 controller.py 的 display 方法签名
   - 接收新的 original_rgb 参数
   - 保存为 `self.current_display_image`

2. ⏳ 修改 tabel_list 方法
   - 使用保存的原始图像裁剪检测框
   - 延迟处理，只在需要时裁剪

---

## 后续任务清单

### 立即需要完成（优先级高）
- [ ] 完成 display 方法的参数接收和保存
- [ ] 测试修改后的性能（预期 postprocess < 50ms）
- [ ] 验证表格显示是否正常

### 如果性能仍不满足
- [ ] 考虑在算法端使用 GPU 加速
- [ ] 考虑批处理推理
- [ ] 优化模型本身

---

## 关键文件修改记录

| 文件 | 修改内容 | 状态 |
|------|--------|------|
| controller/main_controller.py | 修复 partial, 初始化变量, 添加文件夹按钮, 优化 tabel_list | ✅ 部分完成 |
| core/client.py | 修复逗号, 修复拼写, 移除图像裁剪, 添加图像缩放 | ✅ 完成 |
| core/converter.py | 添加原始 RGB 图像传递 | ✅ 完成 |
| core/visualizer.py | 添加字体缓存 | ✅ 完成 |
| core/queue.py | 增加队列大小 | ✅ 完成 |
| ui/mainwindow.ui | 添加文件夹按钮 | ✅ 完成 |
| utils/profiler.py | 新建性能监控工具 | ✅ 完成 |

---

## 代码片段参考

### display 方法需要修改为：
```python
@profiler.measure("display")
def display(self, qimg, details=[], path='', original_rgb=None):
    # 保存原始 RGB 图像供表格使用
    if original_rgb is not None:
        self.current_display_image = original_rgb

    # 保存推理结果
    if self.ui.saveDataButton.isChecked():
        if hasattr(self, 'label_writer'):
            self.label_writer.submit(path, details, drop_if_full=True)

    # ... 后续代码
```

### tabel_list 已修改为延迟裁剪
- 只在需要时从 `self.current_display_image` 裁剪
- 使用保存的 bbox 坐标

---

## 测试步骤

1. 启动程序
2. 选择视频源
3. 选择模型
4. 开始推理
5. 查看日志中的性能数据
6. 预期 postprocess 时间应该从 279ms 降到 < 50ms

# 性能测试说明

## 已完成的优化

### 1. 添加性能监控
- 在关键方法上添加了 `@profiler.measure()` 装饰器
- 监控的方法：
  - `next()` - 帧读取和队列操作
  - `display()` - UI 显示更新
  - `tabel_list()` - 表格更新
  - `postprocess()` - 推理结果后处理
  - API 请求时间
  - JPG 编码时间

### 2. 代码优化
- **字体缓存**：Visualizer 中添加字体缓存，避免重复加载
- **减少颜色转换**：client.py 中已将图像转为 RGB，tabel_list 中移除了重复的 cvtColor 操作
- **移除 rgbSwapped()**：不必要的颜色通道交换

## 如何使用

### 运行测试
1. 启动程序
2. 选择视频源
3. 选择模型
4. 开始推理

### 查看性能日志
在日志输出中会看到：
```
[性能] JPG编码: 15.23ms
[性能] API请求: 245.67ms
[性能] display: 平均 12.34ms (avg of last 10)
[性能] tabel_list: 平均 8.56ms (avg of last 10)
```

## 性能瓶颈判断

### 如果是 QT 界面问题：
- `display` 时间 > 30ms
- `tabel_list` 时间 > 20ms
- 说明主线程 UI 更新太慢

### 如果是算法接口问题：
- `API请求` 时间 > 200ms
- `postprocess` 时间 > 50ms
- 说明推理服务响应慢

### 正常参考值：
- JPG编码: < 10ms
- API请求: < 150ms (取决于模型)
- postprocess: < 30ms
- display: < 20ms
- tabel_list: < 15ms

## 进一步优化建议

### 如果 UI 卡顿：
1. 降低帧率（增加 timer 间隔）
2. 减少表格显示的检测结果数量
3. 使用更快的图像缩放方法

### 如果 API 慢：
1. 优化模型推理速度
2. 使用更小的输入图像
3. 降低 JPEG 质量（当前 80）
4. 增加队列大小允许缓冲
